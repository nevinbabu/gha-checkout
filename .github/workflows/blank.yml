name: checkout-demo
on: 
  push:
    branches:
      - main

jobs:
  checkout-action:
    runs-on: ubuntu-slim
    steps:
      - name: Checkout the code
        uses: actions/checkout@v4
      - name: Print repo code
        run: cat index.html

      
